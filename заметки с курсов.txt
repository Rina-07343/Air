импликация => 
А => В = не А V B
всегда 1 кроме : из 1 => 0 = 0(только 100)
эквиваленция( в питоне ==) 
= (не A V B)/\(A V не B)
когда значения одинаковы тогда ток есть ( 0 и 0; 1 и 1)
порядок: скобки не и или если то равно
формула деморгана минус ко ВСЕМУ ЧТО ЕСТЬ В СКОБКЕ ПРИМЕНЯЕМ + на *, * на +
закон исключенного третьего не А V A= 1 не А /\ A= 0
четные минусы съедаются 

		Функции
	      (№16 и 23)
Процедура и Функция(с return) переписать 
из скринов 
просмотреть скрины 
потестить turtle
		Рекурсия
сам себя вызывает

	ЗАДАНИЯ №17,25,26,27
sum() - сумму всех эл списка
a=[int(input()) for i in range(N)] - запомнить как заполняется список
min(x,y) - выбирает минимальное из 2х аргументов
	ЗАДАНИ7Е №17
for i in range(len(a)-1):
	if a[i]... or a[i+1]:
		.....

n = int(input())
k = 0
a = [int(input()) for i in range(n)]
b = min([i for i in a if i % 2 == 0])
for i in range(n-2):
    if a[i] % b==0 or a[i+1] % b==0 or a[i+2] % b==0:
        k += 1
print(k)          
если есть и -, и +, то надо писать  abs()


	ЗАДАНИЕ №23
	(задачки с рекурсией)
def f(x,y):
 if x>y:
  return 0
 elif x==y:
  return 1
 else: return (f(x+1,y)+f(x+2,y)
print(f(2,14)

